
#Область ОбработчикиСобытий

Процедура ПередНачаломРаботыСистемы(Отказ)
	
	ИнициализацияДанных.ИнициализироватьДанные();		
	
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	
	КомандаМенеджера = ОбщегоНазначенияВызовСервера.КомандаМенеджера();
	
	Если Не ЗначениеЗаполнено(КомандаМенеджера) Тогда
		ОткрытьФорму("ОбщаяФорма.СтартИгры", , , , , , ,
				РежимБлокировкиПриОткрытииОкнаФормы.БлокироватьВесьИнтерфейс);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
